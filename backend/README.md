# Backend - Employee Meal Management System

Java Spring Boot 3 backend application providing RESTful APIs, JWT Security, Spring Data JPA, Flyway migrations, and Apache POI / OpenPDF report generation engines.

## Build and Test

```bash
# Run unit and integration tests
mvn clean test

# Package executable JAR
mvn clean package

# Launch Spring Boot server
mvn spring-boot:run
```
