# Frontend - Employee Meal Management System

React 18 + TypeScript + Vite + Tailwind CSS single page application for employee meal tracking, fast lunchtime recording, role-based dashboards, and report exports.

## Commands

```bash
# Install dependencies
npm install

# Start Vite local development server
npm run dev

# Build production bundle
npm run build
```
